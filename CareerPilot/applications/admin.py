from django.contrib import admin

# Note: This project uses Firebase Firestore for data persistence,
# not Django ORM. These models are kept for reference/future use.
# No models are registered here as the application data is stored in Firestore.
